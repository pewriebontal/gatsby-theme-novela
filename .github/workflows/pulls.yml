name: CI for PR

on:
  pull_request:
    branches:
    - release

jobs:
  CI_Workflow:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Npm Install
      run: npm install
    - name: Npm Lint
      run: npm run lint
    - name: Npm Build
      run: npm run build
